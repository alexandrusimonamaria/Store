Store Management API

CRUD operations
Basic Authentification with 2 roles: - ADMIN - all the CRUD operations & USER(can only see the products)
                                     - add "local" in edit configuration -> active profiles
Using Record for DTO
H2 database: - http://localhost:8080/h2-console/
             - user & pass in application.properties file

Postman test: check in Auth tab that you have username and pass before you try add/get etc.